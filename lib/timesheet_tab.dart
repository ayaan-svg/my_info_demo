import 'package:flutter/material.dart';

class TimesheetTab extends StatelessWidget {
  const TimesheetTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Timesheet and Hours', style: TextStyle(fontSize: 24)),
    );
  }
}
